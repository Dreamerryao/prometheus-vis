const Footer = () => (
  <footer className="container text-gray-400 font-medium flex-auto item-center justify-center">
    <div className="text-center">
      {`prometheus 可视化平台 copyright@2022-${new Date().getFullYear()} powered by @端口已被占用`}
    </div>
  </footer>
);

export default Footer;
